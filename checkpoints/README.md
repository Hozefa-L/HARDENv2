# Checkpoints

This directory stores evaluation metrics and model artifacts organized by
experimental phase.

## Layout

| Path | Contents |
| --- | --- |
| `phase7_balanced/metrics/` | Per-run metric JSON files and test prediction arrays |

The public branch currently exposes the retained Phase 7 metrics slice. The
full serialized model bundle is not tracked on this branch.